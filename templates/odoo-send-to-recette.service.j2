[Unit]
Description=Daily Odoo dump and sync to Recette
After=network.target

[Service]
Type=oneshot
User={{ odoo_username }}
Group={{ odoo_username }}
WorkingDirectory=/home/{{ odoo_username }}/
ExecStart=/home/{{ odoo_username }}/scripts/send_to_recette.sh

[Install]
WantedBy=multi-user.target
